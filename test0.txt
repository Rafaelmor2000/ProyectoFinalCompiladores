program area;
vars
int ancho, largo;
float alto[5];
int algo;

main()
vars 
float otro;
{
    ancho = 5;
    largo = -2;
    ancho = ancho / largo * 5;
    print("el area del rectangulo es: ", ancho * largo > -2 * (ancho + largo) & true);
}