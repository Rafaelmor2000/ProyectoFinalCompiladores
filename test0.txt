program area;
vars
int ancho, largo, otro;
float alto[5];
int algo;

main()
{
    ancho = 5;
    read(otro, largo);
    largo = 1 * 2 * 3;
    write("el area del rectangulo es: ", ancho * largo > 2 * (ancho + largo) & true);
}