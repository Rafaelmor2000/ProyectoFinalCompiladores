program area;
vars
int ancho, otro;
int alto[5], largo[6];
int i, algo;
char uno, dos;

main()
{
    
    for i = 0 to 4 do {
        alto[i] = i;
        largo[i+1] = alto[i] + 1;
        write(largo[i]);
    }

    write("el area del rectangulo es: ", ancho / largo > 2 * (ancho + largo) & true);
}